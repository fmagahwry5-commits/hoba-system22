# ☁️ تشغيل نظام هوبا من أي مكان في العالم (استضافة مجانية)

هذا الدليل يجعل النظام يعمل على الإنترنت برابط خاص بك (مثل `https://hoba.onrender.com`)
تفتحه من **أي جهاز وأي مكان** — الكمبيوتر والموبايل يشتركان في نفس البيانات لحظياً.

**ما سنفعله (≈ 15 دقيقة من الضغطات — كلها مجانية):**

| الخطوة | الخدمة | الغرض | التكلفة |
|--------|--------|-------|---------|
| 1 | **Supabase** | قاعدة البيانات السحابية | مجاناً |
| 2 | **Render** | تشغيل الخادم على الإنترنت | مجاناً |
| 3 | أول دخول | إنشاء حساب المدير | — |

> ⚠️ **قبل أن تبدأ:** تحتاج حساب GitHub (الذي تملك عليه هذا المشروع).

---

## الخطوة 1: قاعدة البيانات على Supabase 🗄️

1. افتح <https://supabase.com> ← **Start your project** ← سجّل الدخول بـ **GitHub**.
2. اضغط **New project** واملأ:
   - **Name**: أي اسم، مثلاً `hoba`
   - **Database Password**: كلمة مرور قوية — **انسخها واحفظها** (لن تحتاجها بعدها، لكن احفظها)
   - **Region**: اختر الأقرب لك (مثلاً `Frankfurt` لمصر والشرق الأوسط)
3. اضغط **Create new project** وانتظر دقيقة أو دقيقتين حتى يجهز.
4. من القائمة اليسرى افتح **SQL Editor** ← **New query**.
5. **انسخ كامل محتوى ملف [`supabase-setup.sql`](./supabase-setup.sql)** من المشروع والصقه في المحرر ← اضغط **Run** ✅ (يجب أن ترى Success).
6. من القائمة اليسرى: **⚙️ Project Settings** ← **API** وانسخ منها:
   - **Project URL** → مثل `https://abcdefgh.supabase.co`
   - من قسم **Project API keys** المفتاح المسمى **`service_role`** (اضغط Reveal لإظهاره) → **احفظه سراً ولا ترسله لأحد** 🔐

---

## الخطوة 2: تشغيل الخادم على Render 🚀

1. افتح <https://render.com> ← **Get Started** ← سجّل الدخول بـ **GitHub** (نفس الحساب).
2. من لوحة التحكم: **New +** ← **Web Service**.
3. اختر المستودع **`hoba-system22`** (قد يطلب إذن الوصول للمستودعات — اسمح به).
4. املأ الإعدادات:
   | الحقل | القيمة |
   |-------|--------|
   | **Name** | `hoba-system` (أو ما يعجبك — سيصبح جزءاً من رابطك) |
   | **Branch** | `arena/019f7c33-hoba-system22` |
   | **Runtime** | `Node` |
   | **Build Command** | *(اتركه فارغاً)* |
   | **Start Command** | `node server.js` |
   | **Instance Type** | `Free` |
5. في قسم **Environment Variables** أضف المتغيرين:

   | Key | Value |
   |-----|-------|
   | `SUPABASE_URL` | رابط مشروعك من الخطوة 1-6 |
   | `SUPABASE_SERVICE_KEY` | مفتاح `service_role` من الخطوة 1-6 |

6. اضغط **Deploy Web Service** وانتظر 1–3 دقائق حتى يظهر `✅ نظام هوبا يعمل الآن` في السجل (Logs).
7. في أعلى الصفحة ستجد رابطك: `https://hoba-system.onrender.com` 🎉

---

## الخطوة 3: أول دخول 🔑

1. افتح رابطك من **أي جهاز** (كمبيوتر المكتب، موبايل الكاشير، موبايلك من البيت — نفس الرابط للجميع).
2. ستظهر **شاشة الإعداد الأولي**: أنشئ حساب **المدير** (يحدث مرة واحدة فقط).
3. من **⚙️ الإعدادات ← المستخدمون**: أضف حسابات الكاشيرين (كل كاشير يبيع ويشوف، والمدير فقط يتحكم بالأسعار والحذف والتقارير).
4. على كل موبايل: من المتصفح اختر **إضافة إلى الشاشة الرئيسية** — سيصبح تطبيقاً بأيقونة على الموبايل 📱.

---

## 💡 ملاحظات مهمة

**الخطة المجانية في Render "تنام" 😴**
بعد 15 دقيقة من عدم الاستخدام تتوقف الخدمة، وأول زيارة بعدها تستغرق ~50 ثانية لإيقاظها.
**الحل المجاني:** سجّل في <https://uptimerobot.com> وأضف مراقباً (Monitor) على رابطك `https://your-app.onrender.com/health` يرسل زيارة كل 5 دقائق — فيظل نظامك مستيقظاً دائماً.

**التحديثات التلقائية 🔄**
Render مرتبط بالمستودع: أي تحديث جديد يُدفع (push) على البرانش يُعيد النشر تلقائياً خلال دقيقتين.

**الأمان 🔐**
- مفتاح `service_role` موجود فقط كمتغير بيئة على السيرفر — لا يظهر أبداً في الواجهة.
- النظام محمي بتسجيل دخول، وكلمات المرور مشفّرة (scrypt)، ومحاولات التخمين تُحظر 5 دقائق بعد 5 محاولات خاطئة.
- Render يعطيك **HTTPS تلقائياً** — وبه تعمل كاميرا مسح الباركود والتثبيت كتطبيق PWA (هذا لا يعمل عبر http على الشبكة المحلية).

**النسخ الاحتياطي 💾**
متوفر داخل النظام: **الإعدادات ← تنزيل نسخة احتياطية** (ملف JSON بكل بياناتك).

**التشغيل المحلي يظل متاحاً 🏠**
لن يتغير شيء محلياً: `node server.js` بدون المتغيرين = وضع الشبكة المنزلية مع ملف `data/db.json`.

> اختيار البرانش: استخدمنا `arena/019f7c33-hoba-system22` لأنه يحوي أحدث الكود. يمكنك أيضاً فتح Pull Request في GitHub ودمجه في `main` ثم اختيار `main` في Render — النتيجة واحدة.
