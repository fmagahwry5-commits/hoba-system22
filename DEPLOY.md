# ☁️ تشغيل نظام هوبا من أي مكان — استضافة مجانية بالكامل

هذا الدليل يجعل النظام يعمل على الإنترنت برابط خاص بك (مثل `https://hoba.vercel.app`)
تفتحه من **أي جهاز وأي مكان** — الكمبيوتر والموبايل يشتركان في نفس البيانات لحظياً.

**كل الخدمات المستخدمة مجانية ولا تطلب بطاقة بنكية:**

| الخطوة | الخدمة | الغرض | الملاحظات |
|--------|--------|-------|-----------|
| 1 | **Supabase** | قاعدة البيانات السحابية | خطة مجانية — بدون بطاقة |
| 2 | **Vercel** | تشغيل النظام على الإنترنت | خطة Hobby مجانية — **بدون بطاقة وبدون "نوم"** ✅ |
| 3 | أول دخول | إنشاء حساب المدير | — |

> ⚠️ **قبل أن تبدأ:** تحتاج حساب GitHub (نفس حساب المشروع).

---

## الخطوة 1: قاعدة البيانات على Supabase 🗄️

1. افتح <https://supabase.com> ← **Start your project** ← سجّل الدخول بـ **GitHub**.
2. اضغط **New project**:
   - **Name**: أي اسم، مثلاً `hoba`
   - **Database Password**: كلمة مرور قوية — احفظها جانباً
   - **Region**: الأقرب لك (مثلاً `Frankfurt` لمصر والشرق الأوسط)
3. اضغط **Create new project** وانتظر دقيقة أو دقيقتين.
4. من القائمة اليسرى: **SQL Editor** ← **New query**.
5. انسخ كامل محتوى ملف [`supabase-setup.sql`](./supabase-setup.sql) والصقه ← **Run** ✅ (يجب أن يظهر Success).
6. من القائمة اليسرى: **⚙️ Project Settings** ← **API** وانسخ:
   - **Project URL** → مثل `https://abcdefgh.supabase.co`
   - مفتاح **`service_role`** (اضغط Reveal) → ⚠️ **يبقى سراً — لا ترسله لأحد**

---

## الخطوة 2: نشر النظام على Vercel 🚀

1. افتح <https://vercel.com> ← **Sign Up** ← **Continue with GitHub** (نفس الحساب).
2. من لوحة التحكم: **Add New…** ← **Project**.
3. اضغط **Import** بجانب مستودع **`hoba-system22`** (قد يطلب إذن الوصول للمستودع — اسمح به).
4. صفحة الإعداد — اترك كل شيء كما هو (Vercel يتعرف تلقائياً على المشروع: الواجهة من `public/` والـ API من `api/`).
5. من قسم **Environment Variables** أضف **3 متغيرات**:

   | Key | Value |
   |-----|-------|
   | `SUPABASE_URL` | رابط مشروعك من الخطوة 1-6 |
   | `SUPABASE_SERVICE_KEY` | مفتاح `service_role` من الخطوة 1-6 |
   | `SESSION_SECRET` | أي نص عشوائي طويل (40 خانة حروف وأرقام مثلاً) — سر توقيع الجلسات |

6. اضغط **Deploy** وانتظر حوالي دقيقة ☕ — عند النجاح ستظهر شاشة Congratulations 🎉
7. اضغط **Continue to Dashboard** ثم **Visit** — رابطك جاهز: `https://<اسم-مشروعك>.vercel.app`

> **اختيار البرانش:** Vercel ينشر افتراضياً من البرانش الرئيسي `main` — إن كان الكود الجديد لم يُدمج بعد في `main`، فعند الإعداد اختر البرانش `arena/019f7c33-hoba-system22` (أو اندمج طلب الدمج المفتوح على GitHub ثم انشر `main` — النتيجة واحدة).

---

## الخطوة 3: أول دخول 🔑

1. افتح رابطك من **أي جهاز** — كمبيوتر المكتب، موبايل الكاشير، موبايلك من البيت.
2. **شاشة الإعداد الأولي**: أنشئ حساب المدير (مرة واحدة فقط).
3. من **⚙️ الإعدادات ← المستخدمون**: أضف حسابات الكاشيرين (الكاشير يبيع ويعرض فقط، المدير يتحكم في كل شيء).
4. على كل موبايل: من المتصفح **إضافة إلى الشاشة الرئيسية** ← يصبح تطبيقاً بأيقونة 📱 (ومنها تعمل كاميرا مسح الباركود تلقائياً لأن الرابط HTTPS).

---

## 💡 ملاحظات — لماذا Vercel مريح أكثر

- **بدون نوم**: لا توقف بعد الخمول ولا حاجة لـ UptimeRobot — يعمل دائماً ✅
- **بدون بطاقة بنكية**: التسجيل بـ GitHub يكفي.
- **تحديث تلقائي**: أي تعديل يُدفَع (push) على البرانش → Vercel تعيد النشر تلقائياً خلال دقيقة.
- **HTTPS تلقائي**: به يعمل تثبيت التطبيق (PWA) ومسح الباركود بالكاميرا.
- **دومين خاص** لاحقاً (اختياري): من إعدادات المشروع ← Domains يمكنك ربط مثل `pos.yourshop.com`.

## 🔐 الأمان

- مفتاح `service_role` و`SESSION_SECRET` يعيشان فقط في متغيرات البيئة على السيرفر — لا يصلان للواجهة أبداً.
- تسجيل دخول إجباري + كلمات مرور مشفّرة scrypt + توكنات جلسات موقّعة HMAC.
- حظر مؤقت بعد 5 محاولات دخول خاطئة.

## 💾 النسخ الاحتياطي

من داخل النظام: **الإعدادات ← تنزيل نسخة احتياطية** (ملف JSON بكل بياناتك — نزّلها دورياً).

## 🏠 الوضع المحلي لا يزال متاحاً

`node server.js` على جهازك بدون أي متغيرات = نظام الشبكة المنزلية بملف `data/db.json` — كما هو تماماً.

---

### ملخص معمارية النشر (للفضوليين)

```
الموبايل / الكمبيوتر (متصفح + PWA)
        │ HTTPS
        ▼
Vercel  ── public/  (الواجهة — تُخدم كملفات ثابتة، مجاناً)
        └── api/index.js  (Serverless Function — نفس كود السيرفر المحلي)
                │ fetch (REST)
                ▼
        Supabase PostgreSQL (الجداول + وظائف SQL الذرّية للفواتير)
```
